// Implement a leading-edge throttle.
//
// Example: const log = throttle(console.log, 100);
//          log('a'); -> fires immediately
//          log('b'); -> ignored (within the 100ms window)
//          // 100ms later
//          log('c'); -> fires (new window)

export function throttle(fn, wait) {
  // Your code here
}
