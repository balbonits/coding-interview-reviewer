// Return true if every opening bracket in s is closed by the same type of
// bracket and they are closed in the correct order.
//
// isValid("()[]{}")  -> true
// isValid("([)]")    -> false

export function isValid(s) {
  // Your code here
}
