// Implement Promise.all from scratch.
//
// promiseAll([1, Promise.resolve(2), 3]) -> resolves to [1, 2, 3]
// promiseAll([Promise.reject('boom')])    -> rejects with 'boom'
// promiseAll([])                          -> resolves to []

export function promiseAll(items) {
  // Your code here
}
