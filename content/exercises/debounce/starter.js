// Returns a function that, when called repeatedly, only invokes `fn` after
// `delay` ms have passed since the last call.
//
// Example:
//   const debounced = debounce(handler, 100);
//   debounced();      // schedules handler for 100ms from now
//   debounced();      // resets the timer
//   // ...100ms after the last call, handler fires ONCE.

export function debounce(fn, delay) {
  // Your code here
}
