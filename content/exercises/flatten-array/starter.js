// Flatten an array up to the given depth.
// Don't use Array.prototype.flat or flatMap.
//
// flatten([1, [2, [3]]])      -> [1, 2, [3]]
// flatten([1, [2, [3]]], 2)   -> [1, 2, 3]

export function flatten(arr, depth = 1) {
  // Your code here
}
