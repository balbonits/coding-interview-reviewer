// asyncPool(limit, tasks):
//   - tasks is an array of thunks (functions returning Promises).
//   - At most `limit` tasks run concurrently.
//   - Resolves with results in original order; rejects on the first error.

export function asyncPool(limit, tasks) {
  // Your code here
}
