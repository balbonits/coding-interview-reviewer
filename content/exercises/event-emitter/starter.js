// Implement a minimal EventEmitter:
//   on(event, listener)      -> returns an unsubscribe function
//   off(event, listener)     -> removes the listener
//   emit(event, ...args)     -> calls every listener
//   once(event, listener)    -> fires at most once, then auto-removes

export class EventEmitter {
  constructor() {
    // Your code here
  }

  on(event, listener) {
    // Your code here
  }

  off(event, listener) {
    // Your code here
  }

  emit(event, ...args) {
    // Your code here
  }

  once(event, listener) {
    // Your code here
  }
}
