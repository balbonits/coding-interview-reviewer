// Render a tag cloud: <ul> of <li><a> with fontSize scaled by weight.
//
// Props: { tags: { label: string, weight: number }[] }
// fontSize = (0.75 + weight * 0.15) + 'em'

export default function TagCloud({ tags }) {
  // Your code here
  return null;
}
