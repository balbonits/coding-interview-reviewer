// memoize(fn, resolver?) -> a function that caches fn's result by arguments.
//
// Example:
//   const slow = (a, b) => a + b;
//   const memo = memoize(slow);
//   memo(1, 2);  // computes
//   memo(1, 2);  // cached

export function memoize(fn, resolver) {
  // Your code here
}
