// Implement JSON.stringify (without calling JSON.stringify).
// Support: null, booleans, numbers, strings, arrays, plain objects, toJSON.
// undefined/functions/symbols become 'null' in arrays, omitted in objects,
// and `undefined` (the value, not the string) at the top level.

export function stringify(value) {
  // Your code here
}
