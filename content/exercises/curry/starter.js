// curry(fn) -> a function that can be called with fewer args at a time,
// invoking fn once enough args have accumulated.
//
// Example: const add = curry((a, b, c) => a + b + c);
//          add(1)(2)(3) === add(1, 2)(3) === 6

export function curry(fn) {
  // Your code here
}
